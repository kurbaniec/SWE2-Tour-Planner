namespace Model
{
    /// <summary>
    /// Enum that represents a Tour type.
    /// </summary>
    public enum Type
    {
        Bike,
        Hike,
        Running,
        Vacation,
        Car,
    }
}